import React from 'react';

const GameScreen: React.FC = () => {
    return (
        <div>
            <h1>Welcome to the Alphabet Game!</h1>
            <p>Tap on the letters and play!</p>
        </div>
    );
};

export default GameScreen;